model Restaurant {
    id        Int      @id @default(autoincrement())
    name      String
    address   String
    bills     Bill[]
    itens     Item[]
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
